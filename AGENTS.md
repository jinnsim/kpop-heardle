# AI agent entry point

If your tool reads `AGENTS.md` instead of `CLAUDE.md`, the actual
instructions are in [`CLAUDE.md`](./CLAUDE.md). Read that file first,
then follow the breadcrumbs to `docs/handoff.md` and `docs/decisions.md`.
